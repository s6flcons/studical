{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
    <h1>Create new Event for {{ calendar.name }}</h1>

    {{ include('event/_form.html.twig') }}

    <a href="{{ path('app_calendar_show', {'id': calendar.id}) }}">back to calendar</a>
{% endblock %}
